# Secrets and credentials
.env.secrets
.env
*.key
*.pem
*.secrets

# Server access information (CRITICAL - NEVER COMMIT)
SERVER-ACCESS.md

# DigitalOcean API tokens (CRITICAL - NEVER COMMIT)
DIGITALOCEAN_API_TOKEN

# Coolify API keys (CRITICAL - NEVER COMMIT)
COOLIFY_ROOT_API_KEY

# SSH keys
baby_clawdbot_key
*.ssh

# Other sensitive data
credentials/
secrets/

# Node modules (should not be committed)
node_modules/
